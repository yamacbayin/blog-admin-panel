import { SortCategoryPipe } from './sort-category.pipe';

describe('SortCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
