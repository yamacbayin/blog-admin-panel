import { SortCommentPipe } from './sort-comment.pipe';

describe('SortCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
