import { FilterCommentPipe } from './filter-comment.pipe';

describe('FilterCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
